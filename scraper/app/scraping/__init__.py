"""Scraping subsystem (placeholder for S0).

This package will implement the URL fetch + extraction pipeline:

  * ``ssrf``   - SSRF guard applied before and during every fetch.
  * ``extract`` - two-phase content extraction (fast metadata, then full text).

No fetch or extraction logic exists in S0; these are structural placeholders.
"""
