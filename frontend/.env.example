# Base URL for the markit HTTP API. Defaults to /api/v1 when unset.
VITE_API_BASE=/api/v1

# Optional Google Identity client id. When empty, the Google sign-in button is
# disabled and the build still succeeds (Google is not required to run markit).
VITE_GOOGLE_CLIENT_ID=
